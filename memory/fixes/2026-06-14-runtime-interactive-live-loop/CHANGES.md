# Changes

- Added immediate terminal printing in interactive `mneme run`.
- Added `--live`, `--live-ticks`, and `--live-sleep-ms`.
- Auto-selects live loop for local speech/vision/lab profiles when no scripted input is provided.
- Added tests for typed interactive output and live speech ticking through a fake command adapter.
- Updated README and runbooks to clarify typed mode versus live perception mode.

