# Context

The user reported that the local UI looked good but only showed `Auto` for camera, microphone, and speaker dropdowns. They also observed that typed chat responses felt like prerecorded messages.

The direct real-device scanner could see devices on the current Mac, so the UI needed a recovery path for empty startup scans and clearer status. The dialogue planner also needed richer deterministic realization without adding model dependencies.
