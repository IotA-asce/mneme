# Risks

- Saved device IDs are only stable if the operating system reports devices consistently.
- `mneme ui` inventories real devices by default, but it still does not open camera/microphone streams.
- The UI feels more present, but it is still a visualization of runtime state, not evidence of sentience.
- Real local speech/vision quality remains dependent on optional dependencies, permissions, and model files.
