# Context

After the Stage 6 foundation, `mneme ui` worked as a local browser dashboard but felt too much like a raw debug page. It also had no way to select the host camera, microphone, or speaker from the UI.

The owner asked for a cleaner minimal UI and device selectors that persist for future browser and terminal sessions.
