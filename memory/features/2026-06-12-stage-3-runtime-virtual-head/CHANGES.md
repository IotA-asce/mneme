# Changes

- Added fake peripheral device and discovery models.
- Added deterministic `MnemeRuntime` and `RuntimeClock`.
- Wired memory, world model, working memory, context windows, attention, executive, dialogue, promotion, extraction, and consolidation daemon in one runtime.
- Added typed user input ingestion and simple explicit-memory candidate creation.
- Added terminal virtual head CLI as `mneme run`.
- Added `mneme-memory` console script for the existing memory CLI.
- Added Stage 3 runtime tests.
- Added virtual-head runbook and updated roadmap/backlog/project memory.
