# Context

The master roadmap revision 2 made the virtual head the next embodiment target and deferred physical hardware, ROS, and actuator work. Stages 0-2 already provided the cognition components; Stage 3 needed one runnable process that wires them together.

This feature implements that wiring without adding real sensors, speech output, platform-specific discovery, or hardware control.
