# Changes

- `src/android_brain_memory/attention.py`: exposure tracking + `_novelty_for`, IOR map + penalty in `_build_target` + recording in `_activate` + purge in `expire`, `_curiosity_state` + `idle_tick`, `_record_history` + `state_history`, new constructor params (`ior_ms`, `ior_penalty`, `enable_curiosity`, `max_history`).
- `tests/test_attention_v1.py` (new): 6 tests.
- `docs/attention/ATTENTION_MANAGER.md` v1 section; `MASTER_ROADMAP.md` M2.3 complete; `REPO_STATUS.md`.
- `implement/attention-v1/` planning files.
