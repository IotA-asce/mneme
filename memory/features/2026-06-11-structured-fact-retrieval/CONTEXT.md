# Context

The repository already supported model validation, SQLite migrations, fact writes, fact support links, and free-text retrieval. The design and roadmap identified structured fact retrieval as the next safe memory prototype task.

This work keeps retrieval deterministic and local. It does not implement embeddings, vector search, consolidation, conflict detection, or the full documented reranking formula.
