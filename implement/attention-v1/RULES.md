# Rules

- Attention publishes state and salience only — never gaze or actuator commands.
- Safety overrides are sacrosanct: IOR and habituation must never delay or weaken a safety target's takeover.
- All v1 behaviors deterministic (injected clock, counter-based rotation); curiosity opt-in to preserve the v0 idle contract.
- Existing v0 tests must pass unmodified.
- Tests first (red).
